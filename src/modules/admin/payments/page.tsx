"use client";
import {ArrowDownRight,ArrowUpRight,ChevronLeft,ChevronRight,CircleDollarSign,Trash2} from "lucide-react";import {useTranslations} from "next-intl";import {useMemo,useState} from "react";
import {DeleteConfirmDialog} from "@/shared/components/dashboard/delete-confirm-dialog";import {SearchInput} from "@/shared/components/dashboard/search-input";import {StatusBadge} from "@/shared/components/dashboard/status-badge";import {Button} from "@/shared/ui/button";import {cn} from "@/shared/lib/utils";import {paymentPreview} from "./payments.seed";import type {PaymentStatus,PaymentTransaction} from "./types";
export function AdminPaymentsPage(){const t=useTranslations("adminPayments");const[rows,setRows]=useState(paymentPreview);const[search,setSearch]=useState("");const[status,setStatus]=useState<"all"|PaymentStatus>("all");const[sort,setSort]=useState<"newest"|"oldest">("newest");const[selected,setSelected]=useState<string[]>([]);const[deleting,setDeleting]=useState<PaymentTransaction>();const[page,setPage]=useState(1);const filtered=useMemo(()=>rows.filter(x=>x.companyName.toLowerCase().includes(search.toLowerCase())&&(status==="all"||x.status===status)).sort((a,b)=>(sort==="newest"?-1:1)*(a.date.localeCompare(b.date))),[rows,search,status,sort]);const pages=Math.max(1,Math.ceil(filtered.length/8));const visible=filtered.slice((page-1)*8,page*8);const incoming=rows.filter(x=>x.direction==="incoming").reduce((s,x)=>s+x.amount,0);const outgoing=rows.filter(x=>x.direction==="outgoing").reduce((s,x)=>s+x.amount,0);return <main className="space-y-6 px-4 py-8 lg:px-8"><header><h1 className="text-3xl font-bold">{t("title")}</h1><p className="mt-2 text-lg text-muted-foreground">{t("subtitle")}</p></header><div className="grid gap-4 md:grid-cols-3"><Summary label={t("summary.incoming")} value={incoming} tone="text-primary bg-primary/10"/><Summary label={t("summary.outgoing")} value={outgoing} tone="text-destructive bg-destructive/10"/><Summary label={t("summary.balance")} value={incoming-outgoing} tone="text-accent-foreground bg-accent" signed/></div><div className="flex flex-col gap-3 md:flex-row md:items-center"><SearchInput label={t("searchLabel")} placeholder={t("searchPlaceholder")} value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}} className="max-w-md"/><select value={sort} onChange={e=>setSort(e.target.value as typeof sort)} aria-label={t("dateFilter")} className="h-11 rounded-lg border bg-card px-4 md:ms-auto"><option value="newest">{t("dates.newest")}</option><option value="oldest">{t("dates.oldest")}</option></select><select value={status} onChange={e=>{setStatus(e.target.value as typeof status);setPage(1)}} aria-label={t("statusFilter")} className="h-11 rounded-lg border bg-card px-4"><option value="all">{t("statuses.all")}</option>{(["completed","failed","refunded"]as PaymentStatus[]).map(x=><option key={x} value={x}>{t(`statuses.${x}`)}</option>)}</select></div><div className="overflow-x-auto rounded-xl border bg-card"><table className="w-full min-w-[760px] text-sm"><thead><tr>{["company","spending","date","status","action"].map((h,i)=><th key={h} className="border-b border-e p-4 text-start font-medium last:border-e-0">{i===0&&<input className="me-3" type="checkbox" aria-label={t("selectAll")} checked={visible.length>0&&visible.every(x=>selected.includes(x.id))} onChange={e=>setSelected(e.target.checked?visible.map(x=>x.id):[])}/>} {t(`columns.${h}`)}</th>)}</tr></thead><tbody>{visible.map(x=><tr className="border-b last:border-0" key={x.id}><td className="p-4"><div className="flex items-center gap-3"><input type="checkbox" aria-label={t("select",{name:x.companyName})} checked={selected.includes(x.id)} onChange={()=>setSelected(v=>v.includes(x.id)?v.filter(id=>id!==x.id):[...v,x.id])}/><div><strong>{x.companyName}</strong><span className="block text-muted-foreground">{x.country}</span></div></div></td><td className={cn("p-4 font-medium",x.direction==="incoming"?"text-success":"text-destructive")}>{x.amount.toLocaleString()}$ {x.direction==="incoming"?<ArrowUpRight className="inline size-4"/>:<ArrowDownRight className="inline size-4"/>}</td><td className="p-4 text-muted-foreground">{new Date(x.date).toLocaleDateString(undefined,{day:"2-digit",month:"short",year:"numeric"})}</td><td className="p-4"><StatusBadge status={x.status} label={t(`statuses.${x.status}`)}/></td><td className="p-4"><Button variant="ghost" size="icon-sm" aria-label={t("delete",{name:x.companyName})} onClick={()=>setDeleting(x)}><Trash2/></Button></td></tr>)}</tbody></table></div><div className="flex items-center justify-between"><Button variant="outline" disabled={page===1} onClick={()=>setPage(page-1)}><ChevronLeft className="rtl:rotate-180"/>{t("pagination.previous")}</Button><span>{t("pagination.page",{page,pages})}</span><Button variant="outline" disabled={page===pages} onClick={()=>setPage(page+1)}>{t("pagination.next")}<ChevronRight className="rtl:rotate-180"/></Button></div><DeleteConfirmDialog isOpen={!!deleting} title={t("deleteDialog.title")} descriptionLine1={t("deleteDialog.description",{name:deleting?.companyName??""})} descriptionLine2={t("deleteDialog.previewWarning")} cancelLabel={t("deleteDialog.cancel")} confirmLabel={t("deleteDialog.confirm")} onClose={()=>setDeleting(undefined)} onConfirm={()=>{if(deleting)setRows(v=>v.filter(x=>x.id!==deleting.id));setDeleting(undefined)}}/></main>}
function Summary({label,value,tone,signed=false}:{label:string;value:number;tone:string;signed?:boolean}){return <section className="flex items-center gap-4 rounded-xl border bg-card p-6"><span className={cn("rounded-full p-3",tone)}><CircleDollarSign/></span><div><p className="text-muted-foreground">{label}</p><strong className="text-2xl">{signed&&value>=0?"+":""}${value.toLocaleString()}</strong></div></section>}
