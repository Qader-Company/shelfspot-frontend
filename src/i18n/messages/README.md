# Translation catalogs

Add one JSON catalog per configured locale, using the locale identifier as the
filename (for example, `<locale>.json`). Supported and default locales are
configured through the environment variables documented in `.env.example`.
